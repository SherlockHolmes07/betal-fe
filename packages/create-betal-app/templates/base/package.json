{
  "name": "{{PROJECT_NAME}}",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "betal-fe": "{{BETAL_FE_VERSION}}"
  },
  "devDependencies": {
    "vite": "{{VITE_VERSION}}"
  }
}
